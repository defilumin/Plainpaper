import React from "react";
import { createRoot } from "react-dom/client";
import Plainpaper from "./App.jsx";

createRoot(document.getElementById("root")).render(
  <React.StrictMode>
    <Plainpaper />
  </React.StrictMode>
);
